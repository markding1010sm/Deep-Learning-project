# Results

Store generated metric tables, plots, and experiment summaries here. Generated CSV, JSON, and PNG
files are ignored by default so that the team can intentionally choose which final figures to add.


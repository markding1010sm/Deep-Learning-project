[data]
dataset_name = "google-research-datasets/go_emotions"
dataset_config = "simplified"
max_length = 128

[model]
checkpoint = "google/distilbert-base-uncased"
num_labels = 28

[training]
batch_size = 16
learning_rate = 0.00002
epochs = 3
weight_decay = 0.01
threshold = 0.50
seed = 42

